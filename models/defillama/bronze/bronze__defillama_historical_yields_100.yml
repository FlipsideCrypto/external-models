version: 2
models:
  - name: bronze__defillama_historical_yields_100
    tests:
      - dbt_utils.unique_combination_of_columns:
            combination_of_columns:
              - DATE
              - POOL_ID
